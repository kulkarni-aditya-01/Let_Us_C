#include <stdio.h>
void prime (int );
int main  ()
{
    int a , num , b ;
    printf("Enter your number to find prime factors : ");
    scanf("%d",&num);
    prime (num);
    printf("Above are the prime factors ");
    return 0;
}
 void prime ( int x)
{
    int kaam ;
    
}